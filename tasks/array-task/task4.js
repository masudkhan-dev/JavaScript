// array
const friends = ["masud", "asif", "sourov", "hasan", "sagor"];

// non array stings
const family = "abba amma";

// non ararry number
const age = 23;

// check array age
console.log(Array.isArray(age));

// check array stings
console.log(Array.isArray(family));

// check array
console.log(Array.isArray(friends));
